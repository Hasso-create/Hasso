[build]
command = "yarn run build"
publish = "out"

[[redirects]]
  from = "/search"
  to = "https://api.mysearch.com"
  status = 200
  force = true 
  headers = {X-From = "Netlify"}
  signed = "API_SIGNATURE_TOKEN"
  "engines": {
  "node": "14.x"
}
